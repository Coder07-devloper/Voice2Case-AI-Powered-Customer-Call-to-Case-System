import { Router } from 'express'; import { auth } from '../middleware/auth.js'; import { addNote, createCase, deleteCase, getCase, listCases, notes, stats, updateCase } from '../controllers/caseController.js'; import { asyncHandler } from '../middleware/asyncHandler.js';
const router = Router(); router.use(auth); router.get('/stats', asyncHandler(stats)); router.get('/', asyncHandler(listCases)); router.post('/', asyncHandler(createCase)); router.get('/:caseId/notes', asyncHandler(notes)); router.post('/:caseId/notes', asyncHandler(addNote)); router.get('/:caseId', asyncHandler(getCase)); router.patch('/:caseId', asyncHandler(updateCase)); router.delete('/:caseId', asyncHandler(deleteCase)); export default router;
